# Quick check: just verify we're in the right directory
# Don't run linting or tests - they're too slow and block commits
# Run these manually: npm run lint && npm test

echo "✅ Pre-commit hook passed - committing changes"
